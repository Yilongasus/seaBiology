VITE_APP_TITLE = '屏東海生館'
VITE_API_BASE_URL = https://sea-biology.zeabur.app/

# 基於node映像進行構建
FROM node:16

# 設置工作目錄
WORKDIR /app

# 複製 package.json 和 package-lock.json
COPY package*.json ./

# 安裝依賴
RUN npm install


# 構建專案
RUN npm run build

# 使用Vite的開發伺服器啟動應用
CMD ["npm", "run", "serve"]